# Aggregated requirements for all notebooks.
# Each section also ships its own requirements.txt with the same pins
# under notebooks/<section>/requirements.txt.

# ---------- Section 01 — Foundations and Traditional Machine Learning ----------
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0

# ---------- Section 02 — Modern Machine Learning Techniques ----------
# Adds gradient boosting via CatBoost (native categorical handling, calibrated probabilities).
catboost>=1.2

# torch (CPU) for the optional powerful-NN exercise in Section 2 (Exercise 4).
torch>=2.0

# ---------- Section 04 — Special Topics in Machine Learning ----------
# Imbalanced classification: SMOTE family and class-imbalance metrics.
imbalanced-learn>=0.12

# CNNs (Section 4.2): torchvision is already pulled in by torch above, but pin explicitly.
torchvision>=0.15
Pillow>=10.0

# Fairness (Section 4.3).
fairlearn>=0.10
folktables>=0.0.12

# ---------- Section 05 — Working with Generative AI: Basics and Best Practices ----------
# OpenAI Python SDK (Responses API).
openai>=1.50
# Local LLM mechanism demo with GPT-2 small via Hugging Face transformers.
# torch (CPU) is already pulled in by Section 02 above.
transformers>=4.40
# Per-notebook API key handling.
python-dotenv>=1.0
# Pillow is already pulled in by Section 04 above (for the multimodal-input demo).
