numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
catboost>=1.2
# torch is needed only for the optional Exercise 4 (powerful tabular NN).
# CPU build is fine — the exercise trains in well under a minute on a laptop.
torch>=2.0
jupyter>=1.0
